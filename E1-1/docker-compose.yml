services:
  web:
    build: .
    ports:
      - "${HOST_PORT}:${CONTAINER_PORT}"
    depends_on:
      - redis
    environment:
      - APP_MODE=production

  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
